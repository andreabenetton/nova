# P-Stratum protocols

P-Stratum consists of a common service plus two peer protocols:

- P-LAP for link adjacency;
- P-RAP for remote association.

Both supply Paths to P-Stratum common through `NOVA-IF-P-PATH-PROVIDER`. Neither is visible directly to R-Stratum.
