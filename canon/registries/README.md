# Registries

These registries reserve names and identifiers. Values are experimental until an ADR is accepted. Removed identifiers remain reserved and are never reused within the same major version.

`obfuscation-profile-identifiers.yaml` assigns profile identifiers used by P-Stratum and R-Stratum. Profile 0 is test-only; production profiles require privacy analysis and an accepted ADR.
