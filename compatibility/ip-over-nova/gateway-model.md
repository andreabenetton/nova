# Gateway model

A routed gateway terminates IP-over-Nova and forwards inner IP datagrams through a separately versioned Gateway Egress interface. End-host Platform Attachments and gateway egress are different boundaries.
