
# Simulated Binding conformance

The Binding must pass the same Provider Conformance Suite as other `NOVA-IF-P-RAP-BINDING` implementations. Additional tests verify deterministic replay and profile-declared capability behavior.
