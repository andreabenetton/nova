
# Invariant rationale

The machine-readable invariants in `interface.yaml` are normative.

P-0AP is added as another provider without adding a new upward Path category. A P-0AP implementation selects an existing modeled Path kind and keeps simulation metadata below P-Stratum common.
