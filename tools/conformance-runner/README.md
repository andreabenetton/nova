# Conformance runner

Planned runtime harness generated from NIDL scenarios. It will run each scenario in provider, consumer, and interoperability modes. The current CI validates scenario structure and reference coverage but does not yet execute components.
